# Compatibility folder

This folder houses the `ipynb` of analysis and the utility code adjusted for machines that do not have `nltk` or the `black` extension installed. It also includes a vignette on graphing the data in `networkx`.
